import { createDefaultRegistry } from '@buildr/components';

/** The component set both routes use: the editor's palette and the canvas's renderer must agree (`manifestHash`). */
export const registry = createDefaultRegistry();
