---
"@next-buildr/core": minor
---

Add effective styles for the inspector (PB-030): `effectiveStyles`, `effectiveStyle` and `hasOverrides` give the value of each style property at a breakpoint and the layer it comes from (desktop → tablet → mobile cascade, per side for box properties).
