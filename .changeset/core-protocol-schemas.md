---
"@buildr/core": minor
---

Adds the editor-canvas protocol schemas to `@buildr/core/protocol`: the envelope, every message of `docs/editor.md`, `parseEditorMessage`/`parseCanvasMessage`/`parseMessage`/`parseEnvelope`, `createMessage`, `MESSAGE_DIRECTION` and `PROTOCOL_VERSION` (PB-065).
