---
"@next-buildr/payload": minor
---

PayloadDataSource (PB-098): `createPayloadDataSource` and `DataQueryError` in `@next-buildr/payload/data` serve queries and media from Payload's Local API with allowlisted collections, fields and sort keys, the reader's access, and the shared `DataSource` contract semantics; new endpoints `POST /buildr/data/query` and `POST /buildr/data/media`.
