---
"@next-buildr/payload": minor
---

Media endpoints (PB-099): `GET /buildr/media` (search, type filter, paging) and `POST /buildr/media` (multipart upload with a required `alt`), with `mediaListQuerySchema` / `mediaListResponseSchema` in the contract.
