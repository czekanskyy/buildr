---
"@buildr/react": minor
---

Canvas: `installForwarding` sends the editor's keyboard shortcuts (`key:down`) and right clicks (`contextmenu`) from the iframe, never from text fields or inline edits (PB-072).
