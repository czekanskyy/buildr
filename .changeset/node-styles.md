---
"@buildr/core": minor
---

Add `node.setStyle` / `node.unsetStyle` / `node.resetStyles` (PB-036): edit a node's styles per layer (desktop, breakpoint, pseudo-state) with values validated by the property grammars, per-side/per-corner support, style locks, empty-object cleanup and history coalescing keys.
