---
"@buildr/editor": minor
---

The style inspector (PB-082): `<StyleInspector />` edits a node's styles per breakpoint from the property registry, refuses values outside the grammar or the theme (`checkStyleInput`), shows values inherited from wider breakpoints and resets one layer at a time.
