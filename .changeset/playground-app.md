---
---

Add the Vite playground app (`apps/playground`) with a fixture gallery, and `renderFixtureToHtml` plus demo components in the private `@buildr/test-utils` (PB-049). No published package changes.
