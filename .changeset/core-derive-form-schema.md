---
"@next-buildr/core": minor
---

Adds `deriveFormSchema(doc, registryMeta, formNodeId)` and its types: the schema a submitted form is checked against, derived from component metadata alone (PB-061).
