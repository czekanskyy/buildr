---
"@next-buildr/components": minor
---

Add the Stack and Grid components with responsive fixtures (PB-052); layout direction, gap, columns and spans are style properties, so they change per breakpoint.
