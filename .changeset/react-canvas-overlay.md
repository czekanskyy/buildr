---
"@buildr/react": minor
---

Canvas: click, double-click, hover and submit capture in edit mode, and a Shadow DOM overlay outlining the selection and hover (per Loop instance), with auto-opening `<details>` and a dev warning for a missing `data-bid` (PB-068).
