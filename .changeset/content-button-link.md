---
"@next-buildr/components": minor
---

Add the Button and Link components (PB-055) and the built-in message catalog (`BUILT_IN_MESSAGES`, `message`): links go through the platform's `Link`, unsafe URLs never become an `href`, and a new-tab link is announced.
