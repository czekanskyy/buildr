---
"@next-buildr/react": minor
---

Canvas drag and drop: `buildHitPath`, `createDndController` (`dnd:over`/`dnd:leave` → `dnd:target`, drop indicator and refusal reason in the overlay, handle-driven moves with autoscroll sending `intent:move`) (PB-070).
