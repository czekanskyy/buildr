---
"@next-buildr/components": minor
---

Add the component library scaffold (PB-050): `<Icon>` with about 90 lucide icons (`ICON_NAMES`, `ICON_NODES`, `hasIcon`), a minimal reset in `@layer buildr.reset` scoped to `.bc-page`, and the `pnpm gen:component` generator.
