---
"@next-buildr/core": minor
---

Adds `createParentTransport` and `createChildTransport` to `@next-buildr/core/protocol`: origin, source, session, version, schema and direction checks on every message, requests with a timeout, replies, and posting to a concrete origin only (PB-066).
