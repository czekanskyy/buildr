---
"@next-buildr/payload": minor
---

Payload plugin scaffold (PB-093): `buildrPlugin` with Zod-validated options, the `layout` / `buildrRevision` / `template` fields, the write-guard (`BUILDR_WRITE`) that keeps the layout safe from non-builder writes, and the `LayoutField` admin component with the "Edit with Visual Builder" button.
