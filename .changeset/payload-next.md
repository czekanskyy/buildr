---
'@buildr/payload': minor
---

`@buildr/payload/next`: `getBuildrDocument` (tagged cache for published reads, uncached drafts), `listPublishedSlugs`, `revalidateHooks`, the cache tag helpers (`tagsFor`, `docTag`, ...) and the SEO mapping (`seoFromDocument`, `alternatesOf`).
