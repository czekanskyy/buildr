---
"@buildr/components": minor
---

Add the Image component (PB-056): media-derived, author-set or decorative alternative text, `srcset` from the generated sizes, display-width presets, fit, focal point and priority, through the platform's `Image`.
