---
"@buildr/components": minor
---

Adds the marketing templates Hero (with `imageRight` and `centered` variants), FeatureGrid, Cta, Testimonial, Pricing, Faq and Contact as `marketingTemplates`, with SVG thumbnails and `marketingTemplateFixtures` (PB-062).
