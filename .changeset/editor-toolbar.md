---
"@buildr/editor": minor
---

Toolbar (PB-083): `<Toolbar>` with the breakpoint switcher, undo/redo, save status, preview and publish actions and links to the CMS and version history. `IconButton` takes a `hint` for the tooltip (shortcuts); `useShortcutHint` and `useOptionalShortcutRegistry` are new.
