---
"@next-buildr/react": minor
---

Canvas: inline text editing on double click for `editor.inlineProp` (static strings), committed once with `inline:commit` on Enter or blur, cancelled with Escape, with the node's view frozen while editing (PB-069).
