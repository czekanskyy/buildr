---
'@buildr/next': minor
---

Locale routing: `isLocale`, `negotiateLocale`, `createLocaleMiddleware`, `generateLocaleStaticParams`, `x-default` in hreflang (`defaultLocale`) and a `locales` option on the preview route.
