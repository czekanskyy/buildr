---
"@next-buildr/core": minor
---

Add core primitives (PB-006): `generateId`/`createSeededIdGenerator` (base62 node IDs), `JsonValue`/`isJsonValue`/`stableStringify`/`hash` (FNV-1a), and `Result`/`Diagnostic` — the shared foundations the rest of `@next-buildr/core` builds on.
