---
"@buildr/editor": minor
---

Clipboard (PB-085): `createClipboard` (copy, cut, paste of fragments as marked text; untrusted reading with `parseClipboardText`; paste through `node.insert` with fresh ids and the first place the rules accept; in-memory fallback), `<ClipboardProvider>` with a live notice, and `useClipboardActions` for the shortcut provider.
