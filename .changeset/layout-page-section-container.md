---
"@next-buildr/components": minor
---

Add the Page, Section and Container components with their fixtures (PB-051), exported from `@next-buildr/components` together with `ComponentFixture` and `FIXTURE_WIDTHS`; their styles are part of `styles.css`.
