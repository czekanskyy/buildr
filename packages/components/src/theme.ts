// The theme the components are designed against: every token their CSS and the templates use
// exists in it. It is the core default, re-exported so an application needs one import.
export { defaultTheme } from '@next-buildr/core';
